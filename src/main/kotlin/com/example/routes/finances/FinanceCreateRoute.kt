package com.example.routes.finances

/*import com.example.dto.CreateFinanceDTO
import com.example.repositories.googleCalendarRepository
import io.ktor.server.application.*
import io.ktor.server.request.*
import io.ktor.server.response.*
import io.ktor.server.routing.*

fun Route.financeCreateRoute() {
  post {
    val body = call.receive<CreateFinanceDTO>()

    val response = googleCalendarRepository.createEvent(
      summary = body.summary,
      startDateString = body.dateString,
      endDateString = body.dateString,
      recurrence = body.recurrence
    )

    call.respond(response)
  }
}*/
